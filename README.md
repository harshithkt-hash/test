# test

A tiny dummy static HTML site, seeded for testing the [pr-review-agent](https://github.com/) locally against real pull requests.

## Structure

- `index.html` -- the page
- `style.css` -- basic styling
- `script.js` -- sets the footer year on load
